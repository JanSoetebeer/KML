# Modulhandbuch classifier — training + inference dependencies.
# Kept separate from the webscraper's requirements so the two can be installed
# independently. Install into the repo-root .venv:
#   .venv/Scripts/python -m pip install -r mlclassifier/requirements.txt
scikit-learn>=1.5,<2.0
numpy>=1.26
scipy>=1.11
pandas>=2.0
joblib>=1.3
PyMuPDF>=1.24        # PDF text extraction (imported as 'fitz')

# OCR fallback for scanned/image PDFs (optional; only needed when OCR_ENABLED=true).
# Also requires the system 'tesseract-ocr' binary + the 'deu' language pack
# (apt-get install tesseract-ocr tesseract-ocr-deu). Absent → OCR degrades to a no-op.
pytesseract>=0.3.10
Pillow>=10.0

# LLM second-stage review (optional; only for mlclassifier/llm_review.py). Uses
# Claude on Amazon Bedrock via the Anthropic SDK's Bedrock client + boto3.
anthropic>=0.40
# boto3 is already required by the webscraper; needed here too for S3 + Bedrock auth.
