.env
logs
state
.venv
output
*.pyc
__pycache__
app.db
Log.txt
webscraper/SYS_AI_MODELS

# Private keys / credentials — never commit
*.pem
*.key

# Stray local artifacts
response.json

# ML classifier: training data (large) + regenerable artifacts.
# The trained model belongs in S3 or is reproduced via `python -m mlclassifier train`.
# training_report.json is kept (small, useful to diff across runs).
modulhandbuecher/
mlclassifier/artifacts/*.joblib
mlclassifier/artifacts/*.jsonl

# Terraform (state can contain secrets; plugins are large)
.terraform/
*.tfstate
*.tfstate.*
*.tfvars
!*.tfvars.example
