scrapy==2.11.2
Twisted==23.10.0
python-dotenv==1.0.1
boto3==1.34.100
botocore==1.34.100
requests==2.32.3
w3lib==2.1.2

# Web application (admin frontend + API)
fastapi==0.111.0
uvicorn[standard]==0.30.1
python-multipart==0.0.9
bcrypt==4.1.3
itsdangerous==2.2.0

# ML classification (optional — only needed when CLASSIFIER_ENABLED=true).
# The ClassificationPipeline degrades to a no-op if these are absent.
# See ../mlclassifier/requirements.txt for the pinned set.
scikit-learn>=1.5,<2.0
pandas>=2.0
joblib>=1.3
PyMuPDF>=1.24
